import React, { PropTypes, Component } from 'react';

class Header extends Component {
    render() {
        return (
            <header>
                <h1>Todo</h1>
            </header>
        )
    }
}

export default Header;
